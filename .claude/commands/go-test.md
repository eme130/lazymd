Run `go test ./...` and analyze results. If tests fail:
1. Read the failing test file and the code it tests
2. Identify the root cause of each failure
3. Propose a fix — either the test expectation is wrong or the code has a bug
4. Apply the fix and re-run tests to confirm

If all tests pass, report the summary (packages tested, time taken).
